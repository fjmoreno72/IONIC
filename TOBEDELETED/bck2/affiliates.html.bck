<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Affiliates - IOCore2 Coverage Analysis Tool</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/test_case.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/sticky-header.css') }}">
    <style>
        /* Add specific styles for affiliates if needed */
        .flag-icon {
            width: 30px; /* Adjust size as needed */
            height: auto;
            vertical-align: middle;
        }
        /* Original column widths (some might be overridden below) */
        .affiliates-table th.flag-column,
        .affiliates-table td.flag-column {
            /* width: 50px; */ /* Overridden below */
            text-align: center;
        }
         .affiliates-table th.type-column,
        .affiliates-table td.type-column {
            /* width: 50px; */ /* Overridden below */
            text-align: center;
        }
         .affiliates-table th.environments-column,
        .affiliates-table td.environments-column {
            /* width: 150px; */ /* Overridden below */
        }
        /* Fix for search icon overlap */
        .search-container input[type="text"] {
            padding-left: 2rem; /* Add space for the icon */
        }
        .search-container .fa-search {
            position: absolute;
            left: 0.75rem; /* Position icon inside the padding */
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d; /* Optional: Adjust icon color */
            pointer-events: none; /* Make icon non-interactive */
        }
        .filter-item.search-container {
            position: relative; /* Needed for absolute positioning of the icon */
        }
        .header-icon {
             width: 24px; /* Adjust size as needed */
             height: auto;
             vertical-align: middle;
             margin-right: 0.5rem; /* Space between icon and text */
        }

        /* Explicit Column Widths for Affiliates Table */
        .affiliates-table th.id-column,
        .affiliates-table td:nth-child(1) { /* ID Column */
            width: 10%;
            min-width: 80px;
        }
        .affiliates-table th[data-sort="name"],
        .affiliates-table td:nth-child(2) { /* Name Column */
            width: 45%;
            min-width: 250px;
        }
        .affiliates-table th.environments-column,
        .affiliates-table td:nth-child(3) { /* Environments Column */
            width: 25%;
            min-width: 150px;
        }
        .affiliates-table th.type-column,
        .affiliates-table td:nth-child(4) { /* Type Column */
            width: 10%;
            min-width: 60px;
            text-align: center;
        }
        .affiliates-table th.flag-column,
        .affiliates-table td:nth-child(5) { /* Flag Column */
            width: 10%;
            min-width: 60px;
            text-align: center;
        }

        /* Styles for column resizing */
        .affiliates-table th { /* Target specific table */
            position: relative; /* Needed for absolute positioning of handle */
            user-select: none; /* Prevent text selection during resize */
        }
        .resize-handle {
            position: absolute;
            top: 0;
            right: 0;
            width: 5px; /* Adjust handle width */
            height: 100%;
            cursor: col-resize;
            z-index: 1; /* Ensure handle is clickable */
        }
        /* Ensure table container allows horizontal scroll */
        .test-cases-table-container { /* Reusing class from test_case.html */
             /* overflow-x: auto; REMOVED - Let Bootstrap handle responsiveness */
        }
        /* Style for sortable headers */
         .affiliates-table th.sortable {
             cursor: pointer;
         }
         .affiliates-table th.sortable:hover {
             background-color: rgba(0,0,0,0.05); /* Subtle hover effect */
         }
         [data-bs-theme="dark"] .affiliates-table th.sortable:hover {
             background-color: rgba(255,255,255,0.05); /* Subtle hover effect */
         }
         .affiliates-table th.sortable i {
             margin-left: 5px;
             color: #aaa; /* Dimmed sort icon */
         }
         [data-bs-theme="dark"] .affiliates-table th.sortable i {
             color: #777;
         }
    </style>
</head>
<body class="logged-in">
    {% include 'nav_menu.html' %}

    <div class="container-fluid px-4">
        <div class="test-cases-container"> {# Reusing class for similar layout #}
            <div class="test-cases-header"> {# Reusing class for similar layout #}
                 <h2><img src="{{ url_for('static', filename='ASC/image/flags/FMN-ASC.png') }}" alt="ASC Icon" class="header-icon">Affiliates</h2> {# Changed icon to image #}
            </div>

            <div class="controls-container">
                <div class="filters-container">
                    <div class="filter-item search-container"> {# Combined search and filter item #}
                        <i class="fas fa-search"></i>
                        <input type="text" id="nameSearchInput" placeholder="Filter by name..." class="form-control form-control-sm">
                    </div>
                    <div class="filter-item">
                        <select id="typeFilter" class="form-select form-select-sm">
                            <option value="">All Types</option>
                            <option value="A">Type A</option>
                            <option value="B">Type B</option>
                            <option value="C">Type C</option>
                            {# Add other types if necessary #}
                        </select>
                    </div>
                    <div class="filter-item">
                        <select id="environmentFilter" class="form-select form-select-sm">
                            <option value="">All Environments</option>
                            {# Environment options will be added dynamically or predefined #}
                            <option value="Static">Static</option>
                            <option value="Deployable">Deployable</option>
                            <option value="Army">Army</option>
                            <option value="Navy">Navy</option>
                            <option value="Joint">Joint</option>
                        </select>
                    </div>
                    {# Add page size selector if needed later #}
                    {# <div class="filter-item">
                        <select id="pageSizeSelector" class="form-select form-select-sm">
                            <option value="25">25 items per page</option>
                            <option value="50">50 items per page</option>
                        </select>
                    </div> #}
                </div>

                <div class="actions-container">
                     <button id="addAffiliateButton" class="btn btn-primary btn-sm" disabled> {# Disabled for now #}
                        <i class="fas fa-plus me-1"></i> Add Affiliate
                    </button>
                </div>
            </div>

            <div class="table-responsive test-cases-table-container"> {# Added test-cases-table-container class #}
                <table class="table table-striped table-hover table-sm affiliates-table test-cases-table"> {# Added affiliates-table class #}
                    <thead>
                        <tr>
                            {# Add checkbox column if needed later #}
                            {# <th class="checkbox-column"><input type="checkbox" id="selectAll"></th> #}
                            <th class="sortable id-column" data-sort="id">ID <i class="fas fa-sort"></i></th>
                            <th class="sortable" data-sort="name">Name <i class="fas fa-sort"></i></th>
                            <th class="sortable environments-column" data-sort="environments">Environments <i class="fas fa-sort"></i></th>
                            <th class="sortable type-column" data-sort="type">Type <i class="fas fa-sort"></i></th>
                            <th class="flag-column">Flag</th> {# Flag column is not sortable #}
                        </tr>
                    </thead>
                    <tbody id="affiliatesTableBody">
                        <!-- Data will be loaded here by JavaScript -->
                    </tbody>
                </table>
                <div id="loadingIndicator" class="loading-indicator" style="display: none;"> {# Initially hidden #}
                    <div class="spinner-border text-primary" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                </div>
                <div id="noResults" class="no-results d-none">
                    <p>No affiliates found matching your criteria.</p>
                </div>
            </div>

            {# --- Pagination Controls (Copied from gps.html) --- #}
            <div class="d-flex justify-content-between align-items-center mt-3" id="paginationControls">
                <div>
                    <label for="itemsPerPageSelect" class="form-label form-label-sm me-2">Items per page:</label>
                    <select id="itemsPerPageSelect" class="form-select form-select-sm d-inline-block" style="width: auto;">
                        <option value="25" selected>25</option>
                        <option value="50">50</option>
                    </select>
                </div>
                <div class="d-flex align-items-center">
                     <span id="pageInfo" class="me-3 text-muted">Page 1 of 1</span>
                     <button id="prevPageButton" class="btn btn-outline-secondary btn-sm me-1" disabled>
                         <i class="fas fa-chevron-left"></i> Previous
                     </button>
                     <button id="nextPageButton" class="btn btn-outline-secondary btn-sm" disabled>
                         Next <i class="fas fa-chevron-right"></i>
                     </button>
                </div>
            </div>
            {# --- End Pagination Controls --- #}

        </div>
    </div>

    <button class="theme-toggle" onclick="toggleTheme()">🌓</button>

    {# Include modal if needed later #}
    {# {% include 'affiliate_modal.html' %} #}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ url_for('static', filename='js/affiliates.js') }}"></script>
</body>
</html>
